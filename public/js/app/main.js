define(["jquery-3.5.1.min", "bootstrap", "swiper.min",
    "slick", "carosoul", "sweetalert", "sticky", "script",
    "compare", "mainindex", "nicescroll", "bucket", "showtrip",
    "lazysizes.min"], function ($) {
    //lazy sizes added
    // var Swiper = require('swiper.min');

    // $(document).ready(function () {

    // });
});
